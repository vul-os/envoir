{
  "//": [
    "RATCHET, not a retreat (2026-08-06). enabling allowJs/checkJs on client/js found real bugs, but",
    "checked-as-a-whole this surface sits at 1174 TS errors across 20 of its 28 .js files plus all 3",
    "test/*.ts files, which would make typecheck:client a permanently-red CI gate. Rather than revert",
    "the work or silence it with @ts-nocheck/any/strict:false, `include` below is scoped down to",
    "EXACTLY the files whose full transitive import closure is error-free right now, verified file by",
    "file with `tsc -p` against a temp include list (not just 'this file has 0 errors of its own' —",
    "TypeScript still type-checks anything an included file imports, clean-looking or not, so a file",
    "with 0 own errors that imports a dirty one makes the whole run red). The rule going forward:",
    "a file (or test) moves INTO include only once ITS OWN errors AND its full import closure are",
    "both zero, and it never moves back out. Do not add a file here by widening a glob — list it",
    "explicitly so a future accidental include is visible in review.",
    "",
    "IN (6 files, exit 0): js/global.d.ts (ambient-only, no logic), js/bus.js (16-line no-op dispatch",
    "table — trivially small, flagged per the founder's own honesty check), js/seed.js (345 lines,",
    "leaf), js/store.js (501 lines, imports only seed.js), js/net/jmap.js (358 lines, leaf),",
    "js/net/send.js (217 lines, imports only store.js -> seed.js).",
    "",
    "OUT despite '0 own errors' — closure-dirty, NOT actually clean, do not re-add without re-checking",
    "the closure: js/app.js (imports onboarding.js, shell.js, pwa.js, identity.js — all dirty below),",
    "js/identity.js (imports safety.js [6 errs] and avatar.js [7 errs]), js/net/sync.js (imports",
    "identity.js, so inherits the same safety.js/avatar.js dirt). The previous agent listed",
    "identity.js and net/sync.js as clean; that was true only per-file, not per-closure — corrected",
    "here after re-measurement.",
    "",
    "OUT, real backlog, error count is that file's own contribution to the full 1174 (files with 0",
    "listed are closure-blocked as above, not error-free): js/avatar.js 7, js/compose.js 61,",
    "js/mesh-sim.js 7, js/mote.js 10, js/onboarding.js 45, js/profileModal.js 17, js/provenance.js 17,",
    "js/pwa.js 15, js/resolver.js 4, js/safety.js 6, js/shell.js 70, js/signin.js 12, js/ui.js 178,",
    "js/views/calendar.js 190, js/views/chat.js 135, js/views/contacts.js 61, js/views/files.js 58,",
    "js/views/groups.js 52, js/views/identity.js 66, js/views/mail.js 85, js/views/settings.js 78.",
    "",
    "OUT, test/*.ts (all 3 — none currently closure-clean): test/i18n.test.ts and test/net.test.ts",
    "both import js/ui.js and js/compose.js directly (net.test.ts also pulls js/net/sync.js);",
    "test/sanitize.test.ts imports js/ui.js and js/identity.js. `npm run test:client` still runs",
    "these at runtime via node's own type-stripping — this tsconfig only gates static typecheck, and",
    "none of the 3 can be typechecked clean until their dirty imports are fixed.",
    "",
    "client/js/*.js stays plain JS on disk — it is served straight to the browser via a bare",
    "<script type=\"module\"> with no bundler in front of it (see client/index.html), and none of",
    "these files are renamed to .ts/.tsx, so the runtime is byte-identical to what shipped before.",
    "checkJs makes tsc read the JSDoc annotations already in each .js file (or infer types from",
    "usage where JSDoc is absent) and enforce them exactly as it would for real .ts — this is the",
    "mechanism, not a build step."
  ],
  "compilerOptions": {
    "strict": true,
    "target": "ES2022",
    "lib": ["ES2022", "DOM"],
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "noEmit": true,
    "allowJs": true,
    "checkJs": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "types": ["node"]
  },
  "include": [
    "js/global.d.ts",
    "js/bus.js",
    "js/seed.js",
    "js/store.js",
    "js/net/jmap.js",
    "js/net/send.js"
  ]
}
