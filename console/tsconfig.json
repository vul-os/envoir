{
  "//": [
    "RATCHET, not a retreat (2026-08-06). enabling allowJs/checkJs on console/js found real bugs, but",
    "checked-as-a-whole this surface sits at 599 TS errors across 14 of its 16 .js files, which would",
    "make typecheck:console a permanently-red CI gate. Rather than revert the work or silence it with",
    "@ts-nocheck/any/strict:false, `include` below is scoped down to EXACTLY the files whose full",
    "transitive import closure is error-free right now, verified with `tsc -p` against a temp include",
    "list (not just 'this file has 0 errors of its own' — TypeScript still type-checks anything an",
    "included file imports, clean-looking or not). The rule going forward: a file moves INTO include",
    "only once ITS OWN errors AND its full import closure are both zero, and it never moves back out.",
    "Do not add a file here by widening a glob — list it explicitly so a future accidental include is",
    "visible in review.",
    "",
    "IN (1 file, exit 0): js/bus.js — a 13-line no-op dispatch table with no imports. This is",
    "trivially small; flagged honestly rather than presented as meaningful coverage. It is the ONLY",
    "file in this surface whose import closure is currently clean.",
    "",
    "OUT despite '0 own errors' — closure-dirty, not actually clean: js/app.js imports store.js,",
    "session.js, setup.js and shell.js, all of which are dirty below, so its closure is dirty even",
    "though app.js itself has no errors of its own.",
    "",
    "OUT, real backlog, error count is that file's own contribution to the full 599 (app.js has 0",
    "listed because it is closure-blocked as above, not error-free): js/crypto.js 11, js/session.js",
    "30, js/setup.js 35, js/shell.js 45, js/store.js 84, js/ui.js 86, js/views/audit.js 7,",
    "js/views/billing.js 10, js/views/directory.js 19, js/views/gateway.js 23, js/views/groups.js 65,",
    "js/views/members.js 85, js/views/overview.js 34, js/views/roles.js 65.",
    "",
    "console/js/*.js stays plain JS on disk — it is served straight to the browser via a bare",
    "<script type=\"module\"> with no bundler in front of it (see console/index.html), and none of",
    "these files are renamed to .ts, so the runtime is byte-identical to what shipped before.",
    "checkJs makes tsc read the JSDoc annotations already in each .js file (or infer types from",
    "usage where JSDoc is absent) and enforce them exactly as it would for real .ts — this is the",
    "mechanism, not a build step. There is no test/ dir here (unlike client/, this surface has no",
    "headless unit tests)."
  ],
  "compilerOptions": {
    "strict": true,
    "target": "ES2022",
    "lib": ["ES2022", "DOM"],
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "noEmit": true,
    "allowJs": true,
    "checkJs": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "types": []
  },
  "include": ["js/bus.js"]
}
