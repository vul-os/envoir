{
  "//": [
    "RATCHET, not a retreat (2026-08-06). enabling allowJs/checkJs on superadmin/js found real bugs,",
    "but checked-as-a-whole this surface sits at 368 TS errors across 8 of its 10 .js files, which",
    "would make typecheck:superadmin a permanently-red CI gate. Rather than revert the work or",
    "silence it with @ts-nocheck/any/strict:false, `include` below is scoped down to EXACTLY the",
    "files whose full transitive import closure is error-free right now, verified with `tsc -p`",
    "against a temp include list (not just 'this file has 0 errors of its own' — TypeScript still",
    "type-checks anything an included file imports, clean-looking or not). The rule going forward: a",
    "file moves INTO include only once ITS OWN errors AND its full import closure are both zero, and",
    "it never moves back out. Do not add a file here by widening a glob — list it explicitly so a",
    "future accidental include is visible in review.",
    "",
    "IN (1 file, exit 0): js/bus.js — a 13-line no-op dispatch table with no imports. This is",
    "trivially small; flagged honestly rather than presented as meaningful coverage. It is the ONLY",
    "file in this surface whose import closure is currently clean.",
    "",
    "OUT despite '0 own errors' — closure-dirty, not actually clean: js/app.js imports store.js and",
    "shell.js, both dirty below, so its closure is dirty even though app.js itself has no errors of",
    "its own.",
    "",
    "OUT, real backlog, error count is that file's own contribution to the full 368 (app.js has 0",
    "listed because it is closure-blocked as above, not error-free): js/shell.js 39, js/store.js 65,",
    "js/ui.js 82, js/views/abuse.js 29, js/views/billing.js 33, js/views/fleet.js 79,",
    "js/views/overview.js 26, js/views/provisioning.js 15.",
    "",
    "superadmin/js/*.js stays plain JS on disk — it is served straight to the browser via a bare",
    "<script type=\"module\"> with no bundler in front of it (see superadmin/index.html), and none of",
    "these files are renamed to .ts, so the runtime is byte-identical to what shipped before.",
    "checkJs makes tsc read the JSDoc annotations already in each .js file (or infer types from",
    "usage where JSDoc is absent) and enforce them exactly as it would for real .ts — this is the",
    "mechanism, not a build step. There is no test/ dir here (unlike client/, this surface has no",
    "headless unit tests)."
  ],
  "compilerOptions": {
    "strict": true,
    "target": "ES2022",
    "lib": ["ES2022", "DOM"],
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "noEmit": true,
    "allowJs": true,
    "checkJs": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "types": []
  },
  "include": ["js/bus.js"]
}
